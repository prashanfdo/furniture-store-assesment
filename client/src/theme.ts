import { Theme } from "@emotion/react";

const theme: Theme = {
  backgroundColor: {
    primary: "red",
    positive: "green",
  },
  breakpoints: ["40em", "52em", "64em", "80em"],
};

export default theme;
