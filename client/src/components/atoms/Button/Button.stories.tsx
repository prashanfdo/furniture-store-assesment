import React from 'react';
import Button from '.';
export default {
  title: 'Atoms/Button',
  component: Button,
};

export const Default = () => <Button />;

export const Primary = () => <Button />;
